onload=principal;

function principal(){
    //concectarse por ajax al servidor, pedir os datos de videojuegos y rellenar la tabla,
    //con etiqueta <template> crea una estructura sin usarla en el html y asi poderla rellenarla con datos
}